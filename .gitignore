node_modules/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# stray wrangler auto-scaffold at the repo root: the REAL worker config is
# server/wrangler.toml. A root config would deploy the whole repo as assets.
wrangler.jsonc
server/.admin-key

# ship pipeline: transient Cloudflare Pages staging dir
.cf-deploy
